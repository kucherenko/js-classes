{
  "name": "lesson-3",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "dependencies": {
    "angular": "^1.5.7"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "budo": "^8.3.0",
    "watchify": "^3.7.0"
  },
  "scripts": {
    "build": "babel src -d lib",
    "preserver": "npm run browserify",
    "server": "budo src/index.js:lib/boundle.js --live --open --watch-glob **/*.{html,css,js} -- -d -t [ babelify --comments false ]",
    "browserify": "browserify -o lib/boundle.js -d -e src/index.js -t [ babelify --comments false ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
